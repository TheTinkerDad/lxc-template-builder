# List the hostnames or IP addresses of the container(s) pulled up using the build template
[containers]
container1.local
container2.local

# Point ansible_ssh_private_key_file to the private counterpart of the public key you've used during building the template
[all:vars]
ansible_user=ansible
ansible_ssh_private_key_file=~/.ssh/ansible-key
host_key_checking=false
